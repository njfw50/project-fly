//nome da variavel       resgatando do DOM o elemento por classe
const h2Class = document.getElementsByClassName('h2-title');
//nome da variavel       resgatando do DOM o elemento por classe
const h2AllSelector = document.querySelectorAll('.h2-title');

// nome da variavel           resgatando do DOM o elemento por id
const h3Id = document.getElementById('h3-title');
// nome da variavel           resgatando do DOM o elemento por id
const h3Selector = document.getElementById('#h3-title');

// Quando usar AllSelector e ElementsClassName ? 
// R: Quando houver necessidade de resgatar mais de 1 um elemento

// Quando usar selector e ElementId ?
// R: Quando houver necessidade de apenas um unico elemento
